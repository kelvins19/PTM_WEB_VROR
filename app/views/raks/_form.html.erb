<%= form_with(model: rak) do |form| %>
  <% if rak.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rak.errors.count, "error") %> prohibited this rak from being saved:</h2>

      <ul>
        <% rak.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nama_rak, style: "display: block" %>
    <%= form.text_field :nama_rak %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
